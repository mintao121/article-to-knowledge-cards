缓存（Cache）是一种临时存储机制，用于把频繁访问的数据放在更快的存储介质上，从而减少对慢速源头（如数据库）的访问。缓存命中（hit）指请求的数据在缓存中找到；未命中（miss）指没找到，需要回源去取。缓存有三个核心难题：失效策略（数据什么时候更新）、一致性（缓存和源数据是否同步）、击穿与雪崩（大量 key 同时失效导致源库压力骤增）。常见的失效策略有 TTL（到时间过期）和 LRU（最近最少使用优先淘汰）。缓存能显著降低延迟、提升吞吐，但会引入数据不一致的风险，需要按业务权衡。
